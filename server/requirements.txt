# Web API
fastapi==0.115.*
uvicorn[standard]==0.32.*

# Rendering
Pillow==11.*

# Vision AI (default provider) — pulls in pydantic, used for structured outputs
anthropic==0.40.*
pydantic==2.*

# Folder watching
watchdog==6.*

# Config
python-dotenv==1.*
