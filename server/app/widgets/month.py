"""THIS MONTH zone — goals / big rocks with progress bars."""


def render(draw, box, data) -> None:
    """Draw monthly goals with progress bars and due dates.

    data = {"goals": [{"text", "progress", "due"}], ...}
    TODO: implement.
    """
    raise NotImplementedError("month widget — phase 5")
