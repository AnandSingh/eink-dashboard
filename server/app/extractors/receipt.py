"""Receipt extractor — receipt photo → expense record.

Example of how smart-classify routing extends to new photo types.
"""


def extract(image_path: str, photo_hash: str, classify_confidence: float) -> None:
    # TODO: extract merchant, total, date; store as an expense record.
    raise NotImplementedError("receipt.extract — later phase")
