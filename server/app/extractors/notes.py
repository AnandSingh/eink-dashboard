"""Notes extractor — whiteboard / freeform notes photo → saved note.

Lower priority than tasks; stub for now.
"""


def extract(image_path: str, photo_hash: str, classify_confidence: float) -> None:
    # TODO: OCR/summarize the note, store as a timestamped note record.
    raise NotImplementedError("notes.extract — later phase")
